---
title: "replaceAsset(file)"
description: "The `replaceAsset` method replaces the current asset with a new file. const fileInput = document.getElementById('fileInput'); const file = fileInput.files[0]; await assetSidebar.replaceAsset(file);"
url: "https://www.contentstack.com/app-sdk---assetsidebarwidget---replaceasset-file-"
product: "Contentstack"
doc_type: "guide"
audience:
  - developers
  - admins
version: "current"
last_updated: "2026-06-26"
---

## replaceAsset(file)

The `replaceAsset` method replaces the current asset with a new file.

```
const fileInput = document.getElementById('fileInput');
const file = fileInput.files[0];
await assetSidebar.replaceAsset(file);
```
