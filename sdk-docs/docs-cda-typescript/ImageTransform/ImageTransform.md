---
title: "ImageTransform"
description: "Image transformations can be performed on images by specifying the desired parameters. The parameters control the specific transformations that will be applied to the image. Example: const url = 'www.example.com'; const transformObj = new ImageTransform().bgColor('cccccc'); const transformURL = url.transform(transformObj);"
url: "https://www.contentstack.com/developers/sdks/content-delivery-sdk/typescript/reference/imagetransform"
product: "Contentstack"
doc_type: "class_intro"
audience:
  - developers
  - admins
version: "current"
last_updated: "2026-06-26"
---

# ImageTransform

## ImageTransform

Image transformations can be performed on images by specifying the desired parameters. The parameters control the specific transformations that will be applied to the image.

**Example:**

```
const url = 'www.example.com';
const transformObj = new ImageTransform().bgColor('cccccc');

const transformURL = url.transform(transformObj);
```
