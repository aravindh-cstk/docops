name: Sync APIDocs → Sandbox

on:
  schedule:
    # Runs daily at 02:00 UTC
    - cron: "0 2 * * *"
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Dry run (no changes applied)"
        type: boolean
        default: false
      skip_export:
        description: "Skip export step (use existing export)"
        type: boolean
        default: false

concurrency:
  group: sync-apidocs-sandbox
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  sync:
    name: Export → Diff → Sync → Validate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        working-directory: tools/migration
        run: npm install

      - name: Install cs-sync dependencies
        working-directory: tools/cs-sync
        run: npm ci

      - name: Lint API docs
        working-directory: tools/migration
        run: npm run lint -- --base origin/main

      - name: Export source stack
        if: ${{ !inputs.skip_export }}
        working-directory: tools/migration
        env:
          SOURCE_STACK_API_KEY: blt8fb40ae1e60d06b9
          SOURCE_STACK_MANAGEMENT_TOKEN: ${{ secrets.SOURCE_STACK_MANAGEMENT_TOKEN }}
          SOURCE_STACK_REGION: us
          SOURCE_STACK_BRANCH: main
          APIDOCS_SANDBOX_STACK_API_KEY: ${{ secrets.APIDOCS_SANDBOX_STACK_API_KEY }}
          APIDOCS_SANDBOX_MANAGEMENT_TOKEN: ${{ secrets.APIDOCS_SANDBOX_MANAGEMENT_TOKEN }}
          APIDOCS_SANDBOX_STACK_REGION: us
          EXPORT_DIR: ./exports
          LOG_LEVEL: info
        run: npm run export

      - name: Diff stacks
        id: diff
        working-directory: tools/migration
        env:
          SOURCE_STACK_API_KEY: blt8fb40ae1e60d06b9
          SOURCE_STACK_REGION: us
          APIDOCS_SANDBOX_STACK_API_KEY: ${{ secrets.APIDOCS_SANDBOX_STACK_API_KEY }}
          APIDOCS_SANDBOX_MANAGEMENT_TOKEN: ${{ secrets.APIDOCS_SANDBOX_MANAGEMENT_TOKEN }}
          APIDOCS_SANDBOX_STACK_REGION: us
          EXPORT_DIR: ./exports
          LOG_LEVEL: info
        run: |
          # Exit 2 = changes found, 0 = in sync, 1 = error
          npm run diff || DIFF_EXIT=$?
          if [ "${DIFF_EXIT:-0}" = "2" ]; then
            echo "has_changes=true" >> "$GITHUB_OUTPUT"
          elif [ "${DIFF_EXIT:-0}" = "0" ]; then
            echo "has_changes=false" >> "$GITHUB_OUTPUT"
          else
            exit ${DIFF_EXIT:-1}
          fi

      - name: Sync to destination
        if: steps.diff.outputs.has_changes == 'true'
        working-directory: tools/migration
        env:
          APIDOCS_SANDBOX_STACK_API_KEY: ${{ secrets.APIDOCS_SANDBOX_STACK_API_KEY }}
          APIDOCS_SANDBOX_MANAGEMENT_TOKEN: ${{ secrets.APIDOCS_SANDBOX_MANAGEMENT_TOKEN }}
          APIDOCS_SANDBOX_STACK_REGION: us
          SYNC_DRY_RUN: ${{ inputs.dry_run || 'false' }}
          EXPORT_DIR: ./exports
          BACKUP_DIR: ./backups
          LOG_LEVEL: info
          MAX_RETRIES: "3"
        run: npm run sync

      - name: Validate sync
        if: steps.diff.outputs.has_changes == 'true' && inputs.dry_run != true
        working-directory: tools/migration
        env:
          APIDOCS_SANDBOX_STACK_API_KEY: ${{ secrets.APIDOCS_SANDBOX_STACK_API_KEY }}
          APIDOCS_SANDBOX_MANAGEMENT_TOKEN: ${{ secrets.APIDOCS_SANDBOX_MANAGEMENT_TOKEN }}
          APIDOCS_SANDBOX_STACK_REGION: us
          EXPORT_DIR: ./exports
          LOG_LEVEL: info
        run: npm run validate

      - name: Upload diff and validation reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: sync-reports-${{ github.run_id }}
          path: |
            tools/migration/exports/diffs/
            tools/migration/exports/reports/
          retention-days: 30
          if-no-files-found: ignore

      - name: Notify on failure
        if: failure()
        uses: actions/github-script@v7
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        with:
          script: |
            const webhookUrl = process.env.SLACK_WEBHOOK_URL;
            if (!webhookUrl) {
              console.log('SLACK_WEBHOOK_URL not set — skipping notification');
              return;
            }
            const payload = {
              text: `*APIDocs → Sandbox sync failed* :x:\nRun: <${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}|#${context.runId}>`
            };
            await fetch(webhookUrl, {
              method: 'POST',
              headers: { 'Content-Type': 'application/json' },
              body: JSON.stringify(payload),
            });
